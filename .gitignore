__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
node_modules/
dist/
.DS_Store
.env
.env.*

# Frontend build output — produced by the Vite build on Frappe Cloud, never committed.
apps/*/*/public/frontend/
apps/*/frontend/auto-imports.d.ts
apps/*/frontend/components.d.ts
apps/*/frontend/*.d.ts
# Frappe Cloud installs with yarn, so the npm lockfile here would be ignored and
# misleading — but yarn.lock is committed, deliberately. Without it FC resolves
# dependency versions freshly on every build, so two deploys of one commit can
# produce different bundles and a transitive update can break a release with no
# code change.
apps/*/frontend/package-lock.json
apps/oneapp/oneapp/www/one.html
apps/oneapp_control/oneapp_control/www/signup.html

# Redis writes its snapshot into whatever directory it was started from.
dump.rdb

# Playwright output.
apps/*/frontend/test-results/

# Throwaway scripts that drive a browser against the dev site — a request
# counter, a probe. They belong beside the frontend because that is where
# Playwright's node_modules is, and nowhere near a commit: one rode in on a
# `git add -A` and had to be taken out again the next commit.
#
# `shot.mjs` is the exception and is the reason there are fewer of these: the
# most common throwaway by far was "take a screenshot of this screen", written
# from scratch every time. It is generated now, so it is committed.
apps/*/frontend/*.mjs
!apps/*/frontend/shot.mjs
apps/*/frontend/playwright-report/
